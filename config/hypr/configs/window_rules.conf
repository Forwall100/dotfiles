# Assign applications to workspaces
#windowrulev2 = workspace 1, class:(LibreWolf)
#windowrulev2 = workspace 2, class:(kitty)
#windowrulev2 = workspace 2, class:(yazi)

# Windows opacity

# Windows tiling

# Windows floating
windowrule = float, class:(LibreWolf)
windowrule = size 860 620, class:(LibreWolf)

windowrule = float, class:(kitty)
windowrule = size 800 550, class:(kitty)

# Prevent suspend when a fullscreen app is open
windowrulev2 = idleinhibit fullscreen, class:^(*)$
windowrulev2 = idleinhibit fullscreen, title:^(*)$
windowrulev2 = idleinhibit fullscreen, fullscreen:1
